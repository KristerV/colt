defmodule Colt.Repo.Migrations.AddKindToIcpLearnings do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:icp_learnings) do
      add :kind, :text, null: false, default: "exclude"
    end
  end

  def down do
    alter table(:icp_learnings) do
      remove :kind
    end
  end
end
